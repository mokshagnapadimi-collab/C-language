#include<stdio.h>
main()
{
	float radius,height,circumference;
	float pi=3.14;
	printf("entre radius of circle");
	scanf("%f",&radius);
	circumference=(2*pi*radius);
	printf("the circumference of circle is %f",circumference);
}
